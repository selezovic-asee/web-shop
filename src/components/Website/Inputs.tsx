import React from "react";

const Inputs = () => {
  return (
    <input
      type="text"
      className="input"
      placeholder="Enter domain name here..."
    />
  );
};

export default Inputs;
